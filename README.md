It's a simple programm for counting numbers unique IPv4 addresses in a file. Here we have two classes with the method countUnique: the first uses BufferedReader, the second uses Stream and 
Files.lines. Both take a file name as String and return a number unique IP as long. Added runtime counting.
